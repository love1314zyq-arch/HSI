有，而且如果从 HSI 的特性出发，你现在这个 PA 确实有几个很自然、而且不一定高风险的升级方向。关键是别做“大
  手术”，而是把现在的“全局高斯噪声原型增强”改成更符合高光谱数据分布的版本。

  你当前 PA 的核心问题其实很明确：

  - prototype 是单中心均值
  - 扰动是各向同性高斯噪声
  - 半径是全局固定的
  - 它没有显式利用 HSI 的两个核心属性：
      - 光谱维相关性强
      - 空间上下文会影响类内分布

  所以你最容易做出“算法创新”的地方，就是把 PA 从通用视觉味道，稍微改成一点 HSI 专属味道。

  我建议你优先考虑下面 4 个方向，按“最稳妥”到“更像创新点”排序。

  ———

  ## 1. 类内协方差感知的原型增强

  你现在的噪声是：

  - proto + N(0, radius^2 I)

  这相当于默认每个特征维度独立、同尺度扰动。
  但高光谱特征通常不是这样，很多维度强相关。

  你可以改成：

  - 每个类估计自己的特征协方差
  - 或只估计对角协方差
  - 扰动从 N(0, Σ_c) 采样，而不是全局球形噪声

  这会变成：

  - Covariance-Aware Prototype Augmentation
  - Class-Distribution Prototype Augmentation

  为什么这对 HSI 合理：

  - HSI 的类内变化本来就带有明显的谱间耦合
  - 各向同性噪声太粗糙
  - 协方差感知增强更像“沿着真实类内分布扩张”

  为什么这个方向稳：

  - 主框架不变
  - 还是原型增强
  - 只是把噪声分布从球形改成类感知分布
  - 很好写成你自己的创新点

  如果你怕风险大，先上：

  - 对角协方差版
    不要一开始就上完整协方差矩阵。

  ———

  ## 2. 光谱子空间约束的原型增强

  这个更有 HSI 味道。

  思路是：

  - prototype augmentation 不应该在整个特征空间里无约束乱扩
  - 而应该在“对当前类最稳定的光谱子空间”里增强

  实现上可以很轻：

  - 对每个类的 feature 做 PCA
  - 保留前 k 个主方向
  - 噪声只沿主方向注入
  - 非主方向噪声弱一些或不加

  这就变成：

  - Spectral-Subspace Prototype Augmentation
  - 中文可以叫：光谱子空间原型增强

  为什么适合 HSI：

  - HSI 的判别性很多时候集中在少数主变化方向上
  - 沿主要谱变异方向做增强，比各向同性乱抖更合理

  这会比“普通 prototype noise”更像专门为高光谱做的设计。

  ———

  ## 3. 空间上下文感知的双原型机制

  这个更像 HSI 专属改造，但也不算特别重。

  当前你每个类只有一个 prototype。
  但 HSI 尤其是 patch-based 输入时，同一类可能有两种来源：

  - 光谱主导样本
  - 空间纹理主导样本

  你可以考虑非常轻量的双原型思路：

  - 每类不是一个 prototype，而是两个 prototype
  - 一个偏光谱稳定中心
  - 一个偏空间上下文中心
  - 增强时随机从两者之一扩张

  这个可以写成：

  - Dual-Prototype Augmentation
  - Spatial-Spectral Prototype Augmentation

  优点：

  - 很像为 HSI 量身定制
  - 能明显区别于原 PASS 的单 prototype 思路

  缺点：

  - 风险比前两个大
  - 需要先验证是否真的比单 prototype 稳

  所以我不建议你第一步就做这个，但它很适合作为论文里的“进一步扩展方向”。

  ———

  ## 4. 原型增强和任务结构绑定

  这个你前面已经隐约走到这一步了，而且很适合你项目。

  具体做法可以是：

  - 单类增量：
      - 原型增强更保守
      - 半径更小
      - 更强调旧类中心稳定
  - 多类增量：
      - 原型增强更积极
      - 半径更大
      - 更强调旧类边界覆盖

  这个方向不一定最“HSI 专属”，但它和你的 spectral3 / SSMA 规律天然统一，特别适合作为你方法的整体特色。

  如果你想同时兼顾：

  - HSI 特性
  - 任务结构特性

  那最好的组合其实是：

  协方差感知 + 任务感知缩放

  也就是：

  - 基础噪声形状由类内协方差决定
  - 整体增强强度由当前任务结构决定

  这就很像一个完整新模块了。

  ———

  # 我最推荐你真正去做的版本

  如果目标是：

  - 不太容易把现在结果搞坏
  - 还能有论文创新点
  - 又确实带 HSI 特征

  我建议你优先做这个：

  ## Task-Aware Spectral Prototype Augmentation

  可以简称：

  - TSPA

  核心设计：

  1. 每类 prototype 仍然保留均值中心
  2. 不再使用单一全局球形噪声
  3. 改成：
      - 每类估计一个轻量谱子空间或对角协方差
  4. 单类增量时缩小增强幅度
  5. 多类增量时正常或略增大增强幅度

  这样你就能把创新点写成：

  - 传统 prototype augmentation 忽略了高光谱类内分布的谱结构
  - 也忽略了不同增量阶段的任务结构差异
  - 因此我们提出一种面向 HSI 的任务感知光谱原型增强机制

  这比单纯说“我们调了个权重”明显强很多。

  ———

  # 如果你现在只想做最小改动

  那我建议按下面优先级：

  1. 先做类对角协方差原型增强

  - 最稳
  - 改动小
  - 很像 HSI 专属增强

  2. 再加任务感知缩放

  - 和你现在的 task-aware auxiliary learning 对齐

  3. 暂时不要一上来做双原型

  - 风险高
  - 更可能影响现有最优结果

  ———

  # 一句话建议

  如果你想让 PA 既有你自己的特色、又不太容易破坏当前效果，最好的改造方向不是“把 prototype 逻辑推翻重来”，
  而是把它升级成：

  面向高光谱类内谱结构的任务感知原型增强

  - 类内分布感知
  - 谱结构感知
  - 任务结构感知

  如果你愿意，我下一步可以直接帮你继续往下做两件更具体的事：

  1. 给这个新 PA 模块起一个正式名字，并写成论文里的创新点段落
  2. 直接给出一版最小可实现代码设计，告诉你该改哪些变量和损失，而不大动现有框架