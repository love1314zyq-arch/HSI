# 任务感知辅助学习方案（2026-03-14）

## 1. 当前核心结论
- 当前项目不再继续追求“单一 SSL 形式统一覆盖所有增量任务”。
- 现阶段更合理、也更符合实验事实的统一方案是：
  - 多类增量任务使用 `SSMA`
  - 单类增量任务使用 `spectral3`

## 2. 结论依据

### 2.1 多类增量任务
- 在多类增量任务上，`SSMA` 已表现出明显潜力。
- 代表性现象：
  - `PaviaU 5+2+2` 在增大 patch 后，`SSMA` 明显优于之前版本，并达到可接受水平
  - `Salinas 8+4+4` 上，`SSMA` 整体表现稳定
- 说明：
  - `SSMA` 更适合作为常规多类增量阶段的增强方式
  - 它更擅长提升模型对多类新样本的鲁棒表征能力

### 2.2 单类增量任务
- 在单类增量任务上，`SSMA` 明显不如 `spectral3`。
- 代表性任务：
  - `PaviaU 8+1`
  - `PaviaU 7+1+1`
  - `Salinas 14+1+1`
  - `Salinas 15+1`
- 当前实验现象一致表明：
  - 单类增量时，`spectral3` 明显更稳
  - `SSMA` 即使在 patch 增大后，也没有解决单类增量掉点问题

## 3. 方法定位
- 因此，后续方法不再表述为：
  - `SSMA` 全面替代 `spectral3`
- 而应统一表述为：
  - 一个 `task-aware auxiliary learning` 框架
  - 中文可表述为：`任务感知辅助学习`
  - 根据当前增量任务结构，选择更合适的辅助目标

## 4. 统一规则
- 若当前增量 task 包含多个新类：
  - 使用 `SSMA`
- 若当前增量 task 只包含 1 个新类：
  - 使用 `spectral3`

## 5. 当前研究判断
- 这个规则不是数据集特化补丁，而是任务结构驱动的统一策略。
- 当前实验支持的核心观点是：
  - 决定辅助学习形式的关键因素，不是数据集名字本身，而是当前增量阶段的新类规模
- 因此，论文与后续实验都应围绕这一点组织：
  - 多类增量：`SSMA`
  - 单类增量：`spectral3`

## 6. 后续执行方向
1. 保留 `step4b` 作为统一对照基线
2. 将后续方案正式收敛为 `task-aware auxiliary learning`
3. 多类增量实验优先验证 `SSMA`
4. 单类增量实验优先验证 `spectral3`
5. 后续论文写法统一为：
   - 固定单一 SSL 目标并不适合所有增量阶段
   - 应根据任务结构自适应选择辅助自监督/增强策略
