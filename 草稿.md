从四个数据集的单类增量实验结果来看，本文方法在最终阶段整体表现出较强的稳定性与竞争力，并在大多数场景下取得了最优结果。具体而言，在 `PaviaU 8+1` 上，本文方法相较于次优方法 FEICA-CIL，在最终阶段的 OA 提升约 `1.25%`，AA 提升约 `2.93%`，Kappa 提升约 `1.66%`；在 `Salinas 15+1` 上，相较于次优方法 FeTrIL，本文方法在 OA、AA 和 Kappa 上分别进一步提升约 `0.28%`、`0.15%` 和 `0.31%`；在 `Houston 14+1` 上，相较于次优方法 FEICA-CIL，本文方法的 OA、AA 和 Kappa 分别提升约 `1.34%`、`1.58%` 和 `1.45%`。这些结果表明，在单类增量场景中，本文方法不仅能够有效吸收新类别信息，而且在旧类保持方面表现出更强的鲁棒性，因此在最终阶段仍能维持较高的整体分类精度和类别均衡性。

`Indian Pines 15+1` 上的结果则呈现出不同于其他数据集的特点。在该数据集上，FeTrIL 的最终 OA 和 Kappa 略高于本文方法，但二者差距均控制在较小范围内，说明本文方法依然保持了较强的竞争力。考虑到 `Indian Pines` 数据集类别分布更不均衡、部分类别样本规模较小且类别间光谱相似性较强，这类场景往往更有利于依赖固定特征空间迁移的策略发挥作用，因此 FeTrIL 在该数据集上取得略优结果是可以理解的。尽管如此，本文方法的 AA 仍保持在较高水平，说明其在整体类别均衡性方面并未出现明显劣势，仍然能够较好地兼顾多数类与少数类的判别性能。换言之，虽然本文方法在 `Indian Pines` 上未取得绝对最优，但其综合表现依旧稳定，且与最优方法之间差距较小，这进一步说明本文方法具有较好的跨数据集泛化能力，而不是仅在个别场景下有效。

结合其余基线结果还可以看出，LwF、SSRE、GFR-IL 与 HyperKD 在多个数据集最终阶段普遍出现了更明显的性能衰减，尤其在 `PaviaU` 和 `Houston` 上与本文方法拉开了较大差距。这说明对于高光谱单类增量任务，仅依赖蒸馏约束、特征迁移或生成式回放的通用增量策略，往往难以同时兼顾新类学习与旧类保持。相比之下，本文方法通过任务感知辅助学习、输入空间回放、特征空间原型增强以及特征级知识蒸馏的协同设计，更有效地缓解了灾难性遗忘，从而在大多数数据集上获得了更优的最终性能。总体而言，该组实验验证了本文方法在高光谱单类增量学习场景中的有效性与稳定性，也说明针对高光谱数据特性构建专门的增量学习机制是十分必要的。

## 多阶段单类增量分析（版本二）

与单次单类增量相比，多阶段单类增量更接近真实的持续学习过程。由于新类别被分散到多个连续阶段逐步引入，模型在每一轮更新中都要处理“新类适应”与“旧类保留”之间的重新平衡，因此最终阶段结果更能反映方法在长序列任务中的累积表现。从最终 task 的对比可以看出，本文方法在 `PaviaU 7+1+1`、`Salinas 14+1+1` 和 `Houston 13+1+1` 三组实验中均排名第一。其中，在 `PaviaU 7+1+1` 上，本文方法相较于次优方法 FEICA-CIL 的 OA、AA 和 Kappa 分别提高约 `0.39%`、`1.31%` 和 `0.52%`；在 `Houston 13+1+1` 上，三项指标相较于 FEICA-CIL 分别提高约 `0.95%`、`1.22%` 和 `1.02%`；在 `Salinas 14+1+1` 上，相较于次优方法 FeTrIL，本文方法在 OA 和 Kappa 上仍分别取得约 `0.33%` 和 `0.37%` 的优势。换言之，当增量过程被拉长之后，本文方法并未出现明显的性能塌陷，而是在多个数据集上依然保持了较高的最终识别水平。

从方法间差异来看，多阶段单类增量首先考验的是“误差是否会在后续任务中持续放大”。LwF、GFR-IL 和 HyperKD 在最终阶段普遍出现较明显的下滑，说明这类方法在面对连续多轮小步更新时，更容易出现旧类边界不断被新类扰动的问题；SSRE 和 FeTrIL 在部分数据集上能保持较好的阶段性结果，但从最终阶段看，其整体表现仍弱于本文方法。`Indian Pines 14+1+1` 的结果则说明，不同数据集的统计特性会改变各方法的相对优势：在该场景下，FEICA-CIL 在最终 OA 和 Kappa 上略优于本文方法，但本文方法仍保持更高的 AA，这表明其在类别间性能分配上更加均衡。考虑到 `Indian Pines` 中类别样本不均衡、部分类别样本量较少且光谱相似类较多，该数据集更容易使某些偏向特定边界建模策略的方法受益。因此，`Indian Pines` 上的结果更适合作为数据集差异性的体现，而不能否定本文方法在其余多数场景中的整体优势。综合来看，多阶段单类增量实验说明，本文方法不仅在单次增量条件下有效，而且在任务链条变长后仍然能够保持较好的最终性能，这为其在更复杂持续学习场景中的应用提供了实验支持。

## 多类增量分析

与单类增量相比，多类增量实验的难点在于每一阶段需要同时接纳多个新类别，模型在一次参数更新中面临更强的新类驱动，因此旧类边界更容易受到扰动。由最终阶段结果可以看出，在这一设置下，不同方法之间的相对优势会随着数据集特性发生变化。总体而言，本文方法与 FEICA-CIL 在四个数据集上均处于最前列，二者构成了多类增量场景中的主要竞争梯队。其中，本文方法在 `Salinas 8+4+4` 上取得了最佳结果，而在 `PaviaU 5+2+2`、`Houston 7+4+4` 和 `Indian Pines 8+4+4` 上，FEICA-CIL 的最终指标略占优势，但本文方法与其差距整体保持在较小范围内，仍然展现出较强的综合竞争力。这说明在多类同时引入的条件下，本文方法依然能够较好地完成新知识吸收与旧知识保持，只是在部分数据集上，面向高光谱增量学习专门设计的特征扩展与压缩机制对复杂类别重组织过程表现出更强的适应性。特别是在 `Salinas 8+4+4` 上，本文方法仍取得了 `99.32%` 的 OA 和 `99.24%` 的 Kappa，说明在类别规模较大、样本较充足且光谱区分较清晰的场景中，本文方法对多类新知识的联合建模能力依然十分突出。

另一方面，多类增量实验也更清楚地揭示了不同类型基线方法在复杂任务切换下的能力边界。LwF、GFR-IL、SSRE 和 HyperKD 在四个数据集上的最终结果整体偏低，说明当单次阶段内引入的类别数增多后，仅依赖蒸馏约束、生成式特征回放或通用迁移机制，往往难以同时兼顾多新类学习与大规模旧类保持。FeTrIL 在单类增量场景中具有一定竞争力，但在多类增量条件下其优势明显减弱，说明冻结特征空间后再进行特征迁移的策略，在新类数量较多时对复杂类间关系的刻画仍然存在局限。相较之下，本文方法虽然并未在所有多类增量场景中均取得绝对最优，但在四个数据集上都维持了较高水平的最终性能，并始终保持在领先梯队内。这表明本文方法在多类增量任务中依然具备较好的泛化能力和鲁棒性，同时也说明，当单次任务引入的类别数进一步增加时，如何进一步增强模型对新旧类别整体结构关系的建模能力，仍然是值得后续深入研究的问题。

进一步观察上述多类增量结果可以发现，`Salinas` 数据集上本文方法取得了最优表现，说明在该数据集上，本文方法对多新类联合引入所带来的特征扰动具有更好的适应能力。基于这一现象，本文进一步选择 `Salinas` 作为代表性数据集，继续研究更复杂的多类增量设置。具体而言，在原有 `8+4+4` 划分的基础上，进一步构造 `8+2+2+2+2` 和 `4+4+4+4` 两种更细粒度的任务划分，以考察当增量阶段数进一步增加、类别分配方式进一步变化时，模型在长期连续学习过程中的性能演化规律。通过这一组更复杂实验，可以更深入地分析本文方法在不同任务组织方式下的适应能力，并验证其在复杂多类增量场景中的鲁棒性与泛化性。

## Salinas复杂多类增量分析

为进一步考察本文方法在更复杂多类增量场景下的适应能力，本文在 `Salinas` 数据集上进一步构造了 `8+2+2+2+2` 与 `4+4+4+4` 两种任务划分。这两种设置分别对应两类更具挑战性的情形：前者通过增加增量阶段数，强化了长期连续学习中的误差累积与遗忘传播问题；后者通过在单个阶段内同时引入更多新类别，显著增加了模型对类间结构重组织的难度。从实验结果可以看出，本文方法在这两种更复杂设置下均取得了最优结果，说明其优势并不依赖于某一种特定任务划分，而是在不同增量粒度下都具有较好的适应能力。

从问题本质来看，复杂多类增量学习的核心困难并不只是“学习新类别”，而是如何在持续引入新类别的过程中保持旧类决策边界的整体稳定。随着任务链条变长，模型容易出现误差逐阶段累积；而当单次新增类别数增多时，特征空间中的类间关系又会被更强烈地重构，导致旧类判别边界更容易被压缩甚至破坏。实验结果表明，部分对比方法虽然在前期阶段能够取得较高精度，但在后续阶段更容易出现明显退化，说明其旧知识保持机制难以同时应对“阶段数增加”和“新类数增加”这两类复杂因素。相比之下，本文方法在两种设置下都保持了更好的最终性能，这说明其不仅能够在短期内吸收新类别信息，还能够在更长的任务序列中持续维持已见类别的整体判别结构。

这一结果也从侧面验证了本文方法设计的合理性。本文并未将增量学习简单地视为分类头扩展问题，而是从任务感知建模、输入空间旧类支撑、特征空间原型补偿以及特征级知识约束等多个层面共同构建旧知识保持机制。这样的设计使模型在面对复杂多类增量任务时，不仅能够缓解灾难性遗忘，还能够更稳定地处理多新类同时引入所带来的类间干扰。因此，`Salinas` 上的进一步实验说明，本文方法在更复杂的多类增量场景中依然具有较好的鲁棒性和泛化能力，也表明针对高光谱数据特性构建多层次协同的增量学习机制是有效且必要的。

## 对比实验小结

综上所述，本文围绕单类增量、多阶段单类增量、多类增量以及更复杂任务划分下的连续学习场景，对所提出方法进行了系统的对比验证。实验结果表明，本文方法在多数数据集和多数任务设置下均取得了优于或接近最优方法的结果，尤其在单类增量和多阶段单类增量场景中表现出更稳定的性能保持能力，能够有效抑制随着任务推进而不断累积的灾难性遗忘。在多类增量场景下，尽管部分专门面向高光谱增量学习的方法在个别数据集上取得了略优结果，但本文方法始终保持在领先梯队，并在更复杂的 `Salinas` 多类增量实验中进一步体现出较强的鲁棒性和泛化能力。

总体来看，这些对比实验说明，本文方法并非仅在某一种固定任务划分下有效，而是在不同数据集、不同增量粒度以及不同任务长度条件下都能够保持较好的综合性能。这表明本文所构建的任务感知辅助学习、输入空间回放、特征空间原型增强以及特征级知识蒸馏等机制具有良好的协同作用，能够同时兼顾新类学习与旧类保持，从而提升模型在高光谱类增量学习场景中的整体稳定性与适应能力。上述结果也进一步验证了针对高光谱数据特性设计专门增量学习框架的必要性和有效性。
