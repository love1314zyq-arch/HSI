# 第三章 方法框架与关键算法设计

## 3.1 问题定义与任务设定

### 3.1.1 高光谱类增量学习任务形式化

高光谱图像分类中的类增量学习（Class-Incremental Learning, CIL）要求模型以阶段式数据流的形式持续习得新类别，同时对已学习的历史类别保持稳定的判别能力。与传统闭集分类范式不同，CIL 中的类别空间并非一次性完整给出，而是随学习进程动态扩展，这从根本上要求模型在历史训练数据不可访问或访问受限的条件下，实现知识的持续积累与有效保持。

**任务序列定义。** 设数据集所涵盖的全部类别集合为 $\mathcal{Y} = \{1, 2, \ldots, C\}$，其中 $C$ 为总类别数。整个训练过程被划分为 $T$ 个有序阶段，构成任务序列 $\{\mathcal{D}_t\}_{t=0}^{T-1}$。第 $t$ 个阶段的训练数据集定义为

$$
\mathcal{D}_t = \left\{ \left( x_n^{(t)},\, y_n^{(t)} \right) \right\}_{n=1}^{N_t},
$$

其中 $x_n^{(t)}$ 为第 $t$ 阶段第 $n$ 个高光谱输入样本，$y_n^{(t)} \in \mathcal{Y}_t$ 为其类别标签，$N_t$ 为该阶段样本总量，$\mathcal{Y}_t$ 为该阶段新引入的类别子集。

**阶段间类别不相交约束。** 各阶段引入的类别集合两两互斥，且其并集覆盖全部类别空间：

$$
\mathcal{Y}_i \cap \mathcal{Y}_j = \varnothing, \quad \forall\, i \neq j, \qquad \bigcup_{t=0}^{T-1} \mathcal{Y}_t = \mathcal{Y}.
$$

其中 $t = 0$ 对应基础训练阶段（base session），用于建立初始判别能力；$t \geq 1$ 对应各增量阶段（incremental session），逐步扩展类别空间。

**测试目标。** 完成第 $t$ 阶段训练后，模型须在所有历史累积类别构成的集合

$$
\mathcal{Y}^{(t)} = \bigcup_{i=0}^{t} \mathcal{Y}_i
$$

上进行联合推理。因此，优化目标并非单纯提升当前新类的分类性能，而是在持续扩展的类别空间 $\mathcal{Y}^{(t)}$ 上维持全局判别稳定性，即同时应对新类学习与旧类保持的双重挑战。

### 3.1.2 任务结构参数化与统一符号约定

**任务划分参数化。** 各阶段新引入类别数记为 $K_t = |\mathcal{Y}_t|$，整个任务序列可由向量

$$
\mathcal{S} = (K_0,\, K_1,\, \ldots,\, K_{T-1})
$$

完整刻画。任务结构对增量学习难度具有显著影响：当 $K_t = 1$（单类增量）时，模型须在极少量样本约束下稳定建立新类特征中心，新类表征的稳定性是核心瓶颈；当 $K_t > 1$（多类增量）时，模型需同时应对多个新类别之间的复杂决策边界，输入空间的多样性建模至关重要。本文后续提出的任务感知辅助学习机制正是基于上述观察所设计。

**统一符号约定。** 输入高光谱样本表示为局部空谱张量

$$
x \in \mathbb{R}^{B \times P \times P},
$$

其中 $B$ 为经主成分分析（PCA）降维后保留的波段数，$P \times P$ 为以目标像素为中心截取的空间邻域大小。设特征提取网络为 $F(\,\cdot\,;\theta_f)$，分类头为 $H(\,\cdot\,;\theta_h)$，则整体预测模型定义为

$$
G(x;\,\theta) = H\!\left(F(x;\theta_f);\,\theta_h\right),
$$

其中 $\theta = \{\theta_f,\,\theta_h\}$ 为全部可学习参数。第 $t$ 阶段第 $n$ 个样本的深层特征表示为

$$
f_n^{(t)} = F\!\left(x_n^{(t)};\,\theta_f\right) \in \mathbb{R}^{d},
$$

$d$ 为特征维度。该向量将作为辅助学习、原型增强、融合回放及分类器优化等各模块的共享输入，是贯穿整个框架的核心表示单元。

### 3.1.3 有范例增量学习设定与核心挑战

本文在**有范例（exemplar-based）**条件下研究上述问题。具体而言，在每个阶段训练结束后，允许从已学习类别中筛选并保留少量具有代表性的原始样本，构建记忆库（memory buffer）$\mathcal{M}^{(t)}$，并在后续增量阶段中与新类数据联合参与模型训练。

有范例设定在提供额外旧类回忆信号的同时，仍面临三个相互关联的核心挑战：

**挑战一（旧类分布不完整性）：** 记忆库容量 $M$ 远小于历史训练数据总量，仅能覆盖旧类分布的极小部分，致使旧类监督信号严重不足。

**挑战二（新旧类样本数量失衡）：** 增量阶段中，新类训练样本数量通常显著多于旧类记忆样本，使分类器产生向新类偏置的系统性倾向，进一步加剧旧类遗忘。

**挑战三（任务结构异质性）：** 不同增量阶段的 $K_t$ 差异显著，统一化的辅助训练策略难以同时兼顾单类增量的表征稳定性需求与多类增量的判别鲁棒性需求。

围绕上述三项挑战，本文从特征建模、任务自适应辅助学习、特征空间旧类补偿、输入空间融合回放及联合训练优化五个维度构建了一套统一的高光谱类增量分类框架，详见后续各节。

---

## 3.2 整体框架设计

### 3.2.1 整体方法流程概述

本文方法的整体流程涵盖数据预处理、阶段式联合训练与旧类知识保持三个相互衔接的环节。

**数据预处理。** 对原始高光谱图像执行 PCA 降维，并以各有标注像素为中心提取 $P \times P$ 局部邻域，得到空谱张量 $x \in \mathbb{R}^{B \times P \times P}$ 作为网络输入。

**阶段式联合训练。** 根据任务序列 $\mathcal{S}$ 将全部类别划分为基础阶段与若干增量阶段，依次驱动模型 $G(x;\theta)$ 进行优化。对于基础阶段 $t = 0$，训练集即为 $\mathcal{D}_0$；对于增量阶段 $t \geq 1$，本文采用融合式回放策略，将记忆库中的旧类样本与当前新类样本直接拼接，构成统一主训练数据流

$$
\widetilde{\mathcal{D}}_t = \mathcal{D}_t \cup \mathcal{M}^{(t-1)}.
$$

旧类样本通过主训练加载器自然参与每轮 mini-batch 迭代，而非以附加损失分支单独输入，从而使旧类监督信号以最直接的方式注入当前优化过程。

**任务感知辅助学习。** 在联合训练中，本文依据当前阶段新增类别数 $K_t$ 显式选择辅助增强策略：$K_t = 1$ 时采用 Spectral3，$K_t > 1$ 时采用 SSMA。该选择在训练流程中显式设定，不依赖模型内部自动学习，保证了辅助学习与任务结构之间的直接对应。

**阶段衔接。** 每阶段训练结束后，模型依次完成三项操作：更新各类别原型与记忆库；将当前模型参数冻结，作为下一阶段的教师模型保留；进入下一增量阶段。

### 3.2.2 网络总体结构

本文整体模型由以下五个功能模块构成：混合 3D-2D 卷积特征提取网络 $F(\,\cdot\,;\theta_f)$、余弦分类头 $H(\,\cdot\,;\theta_h)$、任务感知辅助学习模块、高光谱原型增强模块与融合式原始样本回放模块。

对于任意输入样本 $x$，前向传播过程为

$$
f = F(x;\theta_f) \in \mathbb{R}^{d}, \qquad z = H(f;\theta_h) \in \mathbb{R}^{|\mathcal{Y}^{(t)}|},
$$

其中 $z$ 为类别预测得分向量。训练阶段，$z$ 参与分类监督损失计算；特征向量 $f$ 则进一步参与特征级蒸馏约束与原型增强等模块的联合优化，是整个框架的枢纽表示。

### 3.2.3 各模块协同机制

本文框架的核心设计理念在于围绕"新类高效学习"与"旧类可靠保持"两条优化轴线建立模块间的有机协同。**特征提取网络**为所有下游模块提供统一表示空间；**任务感知辅助学习**在当前阶段强化新类特征的稳定性或边界鲁棒性；**融合式回放**在输入空间中直接注入真实旧类监督；**原型增强**在特征空间中通过分布扰动生成伪旧类样本，补偿记忆库覆盖不足；**特征级知识蒸馏**从深层表示一致性角度全局约束旧类特征的漂移幅度；**余弦分类头**则通过角度空间归一化消除特征模长差异对分类边界的干扰，配合训练机制保障分类稳定性。

上述模块分别作用于输入空间、特征空间与参数空间三个层次，形成多层次、多粒度的旧知识保持体系，同时保证新类知识的充分吸收。

---

## 3.3 混合 3D-2D 卷积特征提取网络

### 3.3.1 输入表示与 Patch 构造

高光谱图像在空间域和光谱域具有高度耦合的信息结构，有效的特征提取须同时捕获局部空间纹理与连续光谱响应模式。为此，本文以像素级局部空谱 patch 作为网络的基本输入单元。

对于数据集中每个带有类别标注的目标像素，以其为空间中心截取大小为 $P \times P$ 的局部邻域，并在光谱维度上执行 PCA 降维，将原始高维波段压缩为 $B$ 个主成分，得到输入张量

$$
x \in \mathbb{R}^{B \times P \times P}.
$$

对于 mini-batch 输入，整体张量形式为

$$
X \in \mathbb{R}^{N \times B \times P \times P},
$$

其中 $N$ 为当前批次样本数。PCA 降维在抑制波段间冗余噪声的同时，显著降低了后续三维卷积的计算开销，在表征质量与运行效率之间取得合理平衡。

### 3.3.2 前端 3D 卷积空谱联合建模

高光谱数据的空谱耦合性在三维结构中得到天然体现，三维卷积核能够在光谱维与空间维上同时滑动，从而显式建模谱间连续过渡与局部空间纹理之间的交互关系。为此，本文在特征提取前端引入两层串联的三维卷积结构。

首先，对批次输入 $X$ 扩展通道维，得到

$$
X' \in \mathbb{R}^{N \times 1 \times B \times P \times P},
$$

作为三维卷积模块所需的标准单通道输入格式。随后，$X'$ 经过两层三维卷积、批归一化（Batch Normalization, BN）与 ReLU 激活，得到中间空谱特征张量

$$
U = F_{\mathrm{3D}}(X';\,\theta_{\mathrm{3D}}) \in \mathbb{R}^{N \times C_{\mathrm{3D}} \times B \times P \times P},
$$

其中 $F_{\mathrm{3D}}(\,\cdot\,)$ 表示前端三维卷积模块，$\theta_{\mathrm{3D}}$ 为其参数，$C_{\mathrm{3D}}$ 为输出通道数。三维卷积核在光谱和空间维度的联合滑动使模型能够在浅层即捕获局部空谱交互模式，为后续深层判别特征的提取奠定基础。

### 3.3.3 后端 2D 卷积深层空间特征提取

经过前端三维卷积后，特征张量 $U$ 保留了显式的光谱深度维，其高维结构不利于直接进行深层特征建模。为此，本文将光谱深度维与通道维合并重整，得到等效二维特征图

$$
V \in \mathbb{R}^{N \times (C_{\mathrm{3D}} \cdot B) \times P \times P}.
$$

该操作将空谱联合表示折叠至通道维，以标准二维特征图格式承接后续模块，在保留光谱编码的同时大幅降低计算复杂度。随后，$V$ 送入二维卷积模块

$$
W = F_{\mathrm{2D}}(V;\,\theta_{\mathrm{2D}}) \in \mathbb{R}^{N \times d \times P \times P},
$$

其中 $F_{\mathrm{2D}}(\,\cdot\,)$ 由两层 $3 \times 3$ 卷积和一层 $1 \times 1$ 通道映射卷积组成，$\theta_{\mathrm{2D}}$ 为对应参数，$d$ 为输出特征维度。二维卷积模块在前端空谱联合表示的基础上进一步强化深层空间判别结构，并将特征统一映射至 $d$ 维标准表示空间，为下游所有模块提供规范化接口。

### 3.3.4 全局平均池化与特征向量生成

在得到空间二维特征图 $W \in \mathbb{R}^{N \times d \times P \times P}$ 后，本文采用全局平均池化（Global Average Pooling, GAP）将空间维压缩为单一向量，得到最终样本特征

$$
f = \operatorname{GAP}(W) \in \mathbb{R}^{N \times d}.
$$

对于单个样本，其特征向量记为 $f_n \in \mathbb{R}^{d}$。GAP 在有效聚合全局空间信息的同时，对空间平移扰动具有天然不变性，有助于提升跨阶段特征的稳定性。该特征向量是后续分类器、知识蒸馏、原型增强和回放模块共享的统一表示。

综上，本文所采用的"前端 3D 卷积建模局部空谱耦合、后端 2D 卷积深化空间判别"的混合骨干设计，在充分挖掘高光谱数据固有空谱结构的同时，兼顾了增量学习场景下对训练效率与特征稳定性的双重需求。

---

## 3.4 任务感知辅助学习策略

### 3.4.1 总体动机与设计原则

高光谱类增量学习的不同阶段在任务结构上存在本质差异，这一差异对模型所面临的学习困难具有显著影响。当 $K_t = 1$ 时，每轮仅引入单一新类，可用新类样本数量极少，新类特征中心的稳定习得是核心瓶颈——此时更需对类内光谱变化进行充分建模，以避免表征对特定光谱条件的过度拟合；当 $K_t > 1$ 时，模型须在有限样本下同时建立多个新类别之间的判别边界，输入空间的扰动多样性与边界泛化能力成为关键。

上述分析表明，单一固定的辅助学习策略难以同时适应两类场景。为此，本文提出**任务感知辅助学习（Task-Aware Auxiliary Learning, TAAL）**策略，根据当前阶段任务结构 $K_t$ 显式选择辅助增强方式。该选择在训练流程中以规则形式预先设定，不依赖模型内部自动学习，保证了策略切换的确定性与可重现性。设第 $t$ 阶段的辅助变换算子为 $\mathcal{A}_t(\,\cdot\,)$，其选择准则定义为

$$
\mathcal{A}_t =
\begin{cases}
\mathcal{A}_{\mathrm{sp}}, & K_t = 1, \\
\mathcal{A}_{\mathrm{ss}}, & K_t > 1,
\end{cases}
$$

其中 $\mathcal{A}_{\mathrm{sp}}$ 为 Spectral3 光谱多视图辅助学习，$\mathcal{A}_{\mathrm{ss}}$ 为空间-光谱混合增强（SSMA）。二者分别针对各自场景的核心困难进行定向设计，无需引入额外参数化选择网络，在任务感知与计算代价之间取得高效平衡。

### 3.4.2 单类增量场景下的 Spectral3 辅助学习

在 $K_t = 1$ 的单类增量场景中，本文设计 **Spectral3** 策略：对同一输入样本 $x$ 构造三种固定的光谱视图，并以标签扩展的方式将其共同用于训练。三种视图定义为

$$
\mathcal{A}_{\mathrm{sp}}(x) = \left\{ x,\; \operatorname{Flip}_{\mathrm{spec}}(x),\; -x + 1 \right\},
$$

其中 $\operatorname{Flip}_{\mathrm{spec}}(\,\cdot\,)$ 表示沿光谱维反转操作，$-x + 1$ 表示光谱幅值取反（像素值关于 $0.5$ 对称翻转）。三种视图从不同角度模拟高光谱数据中的光谱变化性，覆盖了正常、光谱逆序与幅值取反三种典型光谱扰动模式。

与普通数据增强不同，Spectral3 采用标签扩展机制：对于第 $m$ 个视图，其训练标签定义为

$$
\tilde{y}^{(m)} = 3y + (m - 1), \quad m \in \{1, 2, 3\},
$$

即将原始类别 $y$ 映射至扩展标签空间，使三种视图对应三个不同的虚拟子类。因此，分类头的输出维数扩展为 $3|\mathcal{Y}^{(t)}|$，对应特征分别为

$$
f^{(m)} = F\!\left(x^{(m)};\,\theta_f\right), \quad m \in \{1, 2, 3\}.
$$

三组视图特征均参与分类损失计算，等效于在标签空间中为同一类别引入结构化的多样化子视图，迫使模型对不同光谱变化模式建立一致的深层表征。在测试阶段，分类头仍采用扩展维度输出，但仅保留每个原始类别对应的主标签分支（即 $m = 1$ 对应的索引 $3y$）用于最终预测，扩展标签空间不引入额外推断开销。

Spectral3 策略专注于光谱维度的结构化多视图建模，与单类增量场景下"在少样本条件下稳定新类表征"的核心需求高度契合。

### 3.4.3 多类增量场景下的 SSMA 空间-光谱混合增强

在 $K_t > 1$ 的多类增量场景中，本文采用 **SSMA（Spatial-Spectral Mixed Augmentation）** 作为辅助增强策略。与 Spectral3 的光谱结构化多视图不同，SSMA 同时对输入样本的空间结构与光谱信息施加随机扰动，以增强模型对多类复杂边界的泛化能力。

SSMA 增强流程由以下三个步骤的顺序组合构成。首先，对输入样本 $x$ 执行中心裁剪并缩放回原始空间尺寸，得到中间样本 $x'$；随后，以概率 $0.5$ 对 $x'$ 执行水平翻转，引入空间方向随机性；最后，对翻转后的中间样本施加空间-光谱联合随机遮蔽。具体地，生成空间保留掩码 $M_{\mathrm{spa}} \in \{0,1\}^{P \times P}$ 与光谱保留掩码 $M_{\mathrm{spe}} \in \{0,1\}^{B}$，二者经逻辑组合得到混合掩码

$$
M = (1 - M_{\mathrm{spe}}) \odot M_{\mathrm{spa}} + M_{\mathrm{spe}},
$$

其中 $\odot$ 表示逐元素乘积。该组合方式的含义为：对于被光谱掩码保留的波段（$M_{\mathrm{spe}} = 1$），无论空间掩码取值如何，均完整保留原始信息；对于被光谱掩码屏蔽的波段（$M_{\mathrm{spe}} = 0$），进一步由空间掩码决定哪些空间位置被遮蔽。被遮蔽位置以随机常数 $\alpha \sim \mathcal{U}(0,1)$ 填充，从而得到最终增强样本

$$
\hat{x} = M \odot x' + (1 - M) \odot \alpha.
$$

与 Spectral3 不同，SSMA 不改变原始类别标签，增强样本 $\hat{x}$ 仍以原类别索引 $y$ 进行监督学习，不涉及标签空间扩展。

SSMA 通过空间裁剪、随机翻转与空间-光谱联合遮蔽的组合，能够充分探索输入空间的分布多样性，在多类并发学习场景下有效提升模型对新类边界的泛化能力。

### 3.4.4 任务感知辅助学习的整体定位

TAAL 策略以基于 $K_t$ 的轻量化规则实现无额外参数的自适应辅助学习，并以两种策略各自的实现机制（标签扩展 vs. 随机遮蔽）精准对应不同场景的核心困难，避免了统一策略在异质任务结构下的性能退化。

从整体框架的角色分工看，TAAL 主要作用于新类知识的前向学习阶段，与后续旧类保持模块形成功能互补：前者保障新类特征在不同任务结构下的稳定习得，后者将历史知识的遗忘幅度控制在可接受范围，二者共同构成本文框架在新旧类别知识管理上的双轮驱动机制。

---

## 3.5 高光谱原型增强机制

### 3.5.1 类别原型构建

为在增量训练中持续为旧类提供有效监督信号，本文在每个阶段训练结束后，利用当前特征提取网络为各已学习类别构建类别原型（class prototype）。设第 $t$ 阶段结束后，类别 $c \in \mathcal{Y}^{(t)}$ 对应的特征样本集合为

$$
\mathcal{F}_c^{(t)} = \left\{ f_i \mid y_i = c \right\}, \quad f_i = F(x_i;\,\theta_f),
$$

则类别 $c$ 的原型定义为该类特征集合的均值向量：

$$
p_c^{(t)} = \frac{1}{\left|\mathcal{F}_c^{(t)}\right|} \sum_{f_i \in \mathcal{F}_c^{(t)}} f_i \;\in\; \mathbb{R}^{d}.
$$

原型 $p_c^{(t)}$ 作为类别 $c$ 在当前特征空间中的分布中心估计，随模型参数同步更新，以确保其始终准确反映当前特征空间的类别结构，避免因特征漂移导致的原型失效。

### 3.5.2 类内分布建模：对角协方差估计与全局尺度约束

仅依赖原型均值难以完整刻画高光谱类别在特征空间中的类内分布形态。不同高光谱地物在各特征维度上的离散程度差异显著，因此本文进一步对各类别的逐维特征方差进行估计，以捕捉类内分布的各向异性结构。

对类别 $c$ 的逐维方差向量定义为

$$
v_c^{(t)} = \frac{1}{\left|\mathcal{F}_c^{(t)}\right|} \sum_{f_i \in \mathcal{F}_c^{(t)}} \left(f_i - p_c^{(t)}\right) \odot \left(f_i - p_c^{(t)}\right) \;\in\; \mathbb{R}^{d},
$$

其中 $\odot$ 表示 Hadamard 逐元素乘积。对应的对角标准差向量为

$$
\sigma_c^{(t)} = \sqrt{v_c^{(t)} + \delta} \;\in\; \mathbb{R}^{d},
$$

其中 $\delta > 0$ 为数值稳定性常数。

为防止某些维度方差估计退化过小导致扰动失效，本文在基础阶段统计一个全局尺度参考量 $r$，其定义为基础阶段各类别特征协方差迹的均值取平方根，即

$$
r = \sqrt{\frac{1}{|\mathcal{Y}_0|} \sum_{c \in \mathcal{Y}_0} \operatorname{tr}\!\left(\mathrm{Cov}(\mathcal{F}_c^{(0)})\right)}.
$$

在原型扰动采用 spectral-diag 模式时，进一步对各维度标准差施加逐维下界约束：

$$
\sigma_c^{(t)} \leftarrow \max\!\left(\sigma_c^{(t)},\; \rho\, r\right),
$$

其中 $\rho > 0$ 为下界比例系数，用于保证每个维度上的扰动幅度不低于全局特征分布规模的 $\rho$ 倍，从而避免伪样本在低方差维度上过度集中于原型周围，失去类内分布多样性。

### 3.5.3 原型扰动与伪旧类特征生成

在第 $t+1$ 阶段的训练过程中，本文从已保存的旧类原型中采样，在特征空间内生成伪旧类特征（pseudo old-class features），以弥补记忆库真实旧类样本不足带来的旧类分布缺失。

对任意旧类别 $c \in \mathcal{Y}^{(t)}$，其伪旧类特征通过如下各向异性扰动生成：

$$
\tilde{f}_c = p_c^{(t)} + \epsilon \odot \sigma_c^{(t)}, \quad \epsilon \sim \mathcal{N}(\mathbf{0},\, \mathbf{I}_d).
$$

当前实现中，每个 mini-batch 会额外生成与批次大小相同数量的伪旧类特征，各旧类以均匀随机方式被选中参与采样，从而保证每轮迭代中所有历史类别均有机会提供监督信号。

生成的伪旧类特征直接送入分类头计算原型增强损失。值得注意的是，在 Spectral3 辅助模式下，分类头输出维数被扩展为 $3|\mathcal{Y}^{(t)}|$，对应旧类的伪特征标签须映射至扩展标签空间中的主标签索引（即 $\tilde{y} = 3c$）；在 SSMA 辅助模式下，分类头维数保持 $|\mathcal{Y}^{(t)}|$，旧类标签直接使用原始类别索引。原型增强损失统一定义为

$$
\mathcal{L}_{\mathrm{pa}} = \frac{1}{|\widetilde{\mathcal{F}}|} \sum_{(\tilde{f}_c,\, \tilde{c})\, \in\, \widetilde{\mathcal{F}}} \ell\!\left(H(\tilde{f}_c;\,\theta_h),\; \tilde{c}\right),
$$

其中 $\widetilde{\mathcal{F}}$ 为当前 mini-batch 中生成的伪旧类特征集合，$\tilde{c}$ 为与当前辅助模式对应的训练标签索引，$\ell(\,\cdot\,)$ 为交叉熵分类损失。

### 3.5.4 原型增强在整体框架中的定位

原型增强机制的本质在于以极低的存储开销（每类仅需保存均值向量 $p_c^{(t)}$、方差向量 $v_c^{(t)}$ 及全局尺度 $r$）在特征空间中为旧类提供结构化的监督扩充。由于伪样本在整体上遵循类别的各向异性分布结构，其监督信号比仅依赖原型均值的简单约束更为丰富，能够有效激活分类头对旧类决策超平面的记忆。

从模块分工看，原型增强对应"特征空间旧类补偿"，融合式回放对应"输入空间旧类回忆"，二者分别在不同抽象层次提供旧类监督，相辅相成，共同构成本文旧知识保持体系的双层防线。

---

## 3.6 融合式原始样本回放机制

### 3.6.1 记忆库构建与自适应预算分配

有范例设定的核心在于维护一个高质量的记忆库 $\mathcal{M}^{(t)}$，使其在有限存储预算下尽可能保留各历史类别的分布信息。本文采用**自适应固定总预算**策略管理记忆库：设记忆库总容量上限为 $M$，在第 $t$ 阶段结束后，系统需在 $|\mathcal{Y}^{(t)}|$ 个历史类别之间重新分配存储预算。

与简单均匀分配不同，本文当前实现显式考虑各类别的当前训练样本规模：对每个类别 $c$，系统首先计算其理论可分配上限，再结合每类最小保留样本数 $m_{\min}$ 与"当前训练集样本数小于给定阈值时全量保留"的规则，综合确定各类实际保留上限 $m_c^{(t)}$，进而裁剪和更新对应的记忆内容。该机制在类别数随增量阶段持续增长时，既能防止个别类别因预算重分配而被完全清除，又能将有限存储资源优先分配给样本规模较大、分布更难覆盖的类别。

### 3.6.2 基于 Herding 的代表样本筛选

在确定各类存储预算后，需从候选样本集中选取最具代表性的子集存入记忆库。本文采用 **Herding 选择策略**：对类别 $c$ 的候选集 $\mathcal{D}_c$，首先利用当前特征提取网络提取各样本的深层特征，并计算类别原型 $p_c^{(t)}$；随后，按样本特征与原型之间距离由近及远依序选取，直至达到该类记忆预算上限 $m_c^{(t)}$，构成记忆子集 $\mathcal{M}_c^{(t)}$。整体记忆库为各类记忆子集的并集：

$$
\mathcal{M}^{(t)} = \bigcup_{c\, \in\, \mathcal{Y}^{(t)}} \mathcal{M}_c^{(t)}.
$$

Herding 策略倾向于保留特征空间中最贴近类别中心的样本，能够在有限预算下有效捕获各历史类别分布的核心结构，与基于原型均值的记忆筛选目标天然一致。

### 3.6.3 融合式联合训练（Merged Replay）

本文采用**融合式回放（Merged Replay）**策略，在每个增量阶段 $t \geq 1$ 的训练开始前，将记忆库样本与当前新类样本直接合并为统一训练集：

$$
\widetilde{\mathcal{D}}_t = \mathcal{D}_t \cup \mathcal{M}^{(t-1)}.
$$

这一设计的关键在于：旧类样本通过主训练数据加载器自然参与每个 mini-batch 的迭代，而非以独立 replay loss 支路的形式附加输入。从而，旧类样本在每步参数更新中与新类样本地位完全平等，模型在主优化流中同时接受来自新旧类别的监督信号。

对融合训练集 $\widetilde{\mathcal{D}}_t$ 中的任意样本 $(x, y)$，经辅助增强变换 $\mathcal{A}_t(\,\cdot\,)$ 后的分类损失定义为

$$
\mathcal{L}_{\mathrm{cls}} = \frac{1}{|\widetilde{\mathcal{D}}_t|} \sum_{(x,\, y)\, \in\, \widetilde{\mathcal{D}}_t} \ell\!\left(\frac{H\!\left(F(\mathcal{A}_t(x);\,\theta_f);\,\theta_h\right)}{\tau},\; \tilde{y}\right),
$$

其中 $\tau$ 为温度参数，$\tilde{y}$ 为与当前辅助学习模式对应的训练标签（Spectral3 模式下为扩展标签，SSMA 模式下为原始类别标签）。该设计在消除新旧类样本优化地位差异的同时，将辅助学习变换自然融入主训练流，避免了多支路设计带来的优化权衡困难。

### 3.6.4 回放机制在整体框架中的定位

融合式回放在**输入空间**层面为模型直接提供真实旧类样本，驱动特征提取网络和分类头在原始像素层面保持对历史类别的识别能力。相较于将旧类监督以附加损失形式引入，Merged Replay 策略在实现上更为简洁，在优化语义上也更为自然：旧类监督不再是主训练过程的"补丁"，而是有机融入每一步参数更新。结合原型增强在特征空间层面的旧类分布补偿，二者共同构成本文旧知识保持体系在不同抽象层次上的协同防线。

---

## 3.7 增量训练优化与分类器设计

### 3.7.1 特征级知识蒸馏约束

灾难性遗忘的本质是新阶段的参数更新破坏了特征提取网络在旧类上的响应稳定性。为从深层表示一致性角度抑制这种漂移，本文在每个增量阶段 $t \geq 1$ 保留上一阶段训练完成后的冻结参考模型（教师模型）$G^{(t-1)}(\,\cdot\,;\,\theta^{\mathrm{old}})$。

与基于 soft logits 的输出层蒸馏不同，本文当前实现直接约束新旧模型的**深层特征表示**，以更直接地控制特征空间漂移。设输入样本 $x$ 经当前阶段辅助变换后记为 $\bar{x} = \mathcal{A}_t(x)$，教师模型与学生模型（当前模型）分别提取其特征：

$$
f^{\mathrm{old}} = F(\bar{x};\,\theta_f^{\mathrm{old}}), \qquad f = F(\bar{x};\,\theta_f).
$$

特征级蒸馏损失定义为两者之间的 $\ell_2$ 距离：

$$
\mathcal{L}_{\mathrm{kd}} = \left\| f - f^{\mathrm{old}} \right\|_2.
$$

通过最小化 $\mathcal{L}_{\mathrm{kd}}$，学生模型的特征提取行为被显式约束在与教师模型一致的表示空间内，从而在吸收新类知识的同时全局抑制旧类特征的漂移。相较于 logit 层蒸馏，特征级蒸馏能够更直接地作用于网络对旧类的内部表征，对增量学习中特征空间的稳定性保障更为有效。

### 3.7.2 联合优化目标

综合主分类损失、特征级蒸馏约束与原型增强损失，本文第 $t$ 阶段的完整联合优化目标为

$$
\mathcal{L} = \mathcal{L}_{\mathrm{cls}} + \lambda_{\mathrm{kd}}\,\mathcal{L}_{\mathrm{kd}} + \lambda_{\mathrm{pa}}\,\mathcal{L}_{\mathrm{pa}},
$$

其中 $\lambda_{\mathrm{kd}} > 0$ 与 $\lambda_{\mathrm{pa}} > 0$ 分别为蒸馏损失与原型增强损失的平衡系数。三项损失在各自层面协同作用：$\mathcal{L}_{\mathrm{cls}}$ 在融合训练集上建立新旧类别的联合判别边界；$\mathcal{L}_{\mathrm{kd}}$ 在特征空间层面全局约束旧类知识漂移；$\mathcal{L}_{\mathrm{pa}}$ 通过伪旧类特征强化分类头对旧类决策超平面的记忆。

由于融合式回放已将旧类真实样本纳入主训练流 $\widetilde{\mathcal{D}}_t$，三项损失均可在同一前向计算中共享特征提取结果，无需额外的旧类样本支路，整体训练流程简洁高效。

### 3.7.3 余弦归一化分类器

在分类器设计上，本文采用基于余弦相似度的归一化分类头。设样本特征为 $f \in \mathbb{R}^{d}$，类别 $c$ 对应的权重向量为 $w_c \in \mathbb{R}^{d}$，则类别得分定义为

$$
z_c = s \cdot \frac{f^\top w_c}{\|f\|_2\, \|w_c\|_2},
$$

其中 $s > 0$ 为缩放因子。对所有类别构成的预测向量 $z = [z_1, z_2, \ldots, z_{|\mathcal{Y}^{(t)}|}]$，通过 Softmax 映射为最终类别概率分布。

余弦分类器将特征向量与权重向量均投影至单位超球面，以角度距离度量类别相似性，从而消除特征模长差异对分类决策的干扰。在增量学习场景下，各阶段新增类别的权重向量被动态追加至分类头，余弦归一化机制使不同阶段引入的新旧类别权重在同一尺度空间内可比，有效避免了因不同阶段特征幅值不一致引发的分类边界紊乱，提升了类别空间持续扩展过程中的判别稳定性。

### 3.7.4 分类器偏置校准说明

针对增量学习中新旧类样本数量失衡引发的分类器偏置问题，本文代码框架中保留了后校准接口：当分类头为普通线性分类器时，可通过对新类权重范数进行缩放以匹配旧类平均范数，从而缓解新类偏置。

然而，本文主实验采用的是余弦归一化分类器，其前向推理过程中特征与分类权重均会执行 $\ell_2$ 归一化，权重向量的绝对模长不直接影响分类得分的相对大小。因此，线性权重对齐接口在当前主设置下不参与实际参数更新。本文框架中的分类稳定性主要源自余弦分类器本身的归一化机制，以及特征级蒸馏、融合式回放与原型增强三者的协同约束，而非依赖额外的权重后处理步骤。

---

## 3.8 本章小结

本章围绕有范例条件下的高光谱类增量分类问题，系统阐述了本文所提方法的任务形式化定义、整体框架设计与各关键模块的技术细节。

在任务定义层面，本章给出了高光谱类增量学习的严格数学形式化表述，明确了阶段间类别不相交约束、累积类别空间 $\mathcal{Y}^{(t)}$ 上的统一测试目标，以及有范例设定下记忆库 $\mathcal{M}^{(t)}$ 的使用约定，并从旧类分布不完整性、新旧样本数量失衡与任务结构异质性三个维度系统分析了该问题的核心挑战。

在框架设计层面，本章提出了以混合 3D-2D 卷积网络为特征提取骨干、以任务感知辅助学习（TAAL）为新类学习驱动、以高光谱原型增强与融合式回放（Merged Replay）为旧类保持双层支撑、以特征级知识蒸馏为表示空间一致性约束、以余弦归一化分类器为增量分类决策基础的统一框架。各模块作用于输入空间、特征空间与参数空间三个层次，在功能上形成多层次协同，在目标上共同指向新旧类知识的均衡管理。特别地，Spectral3 的固定三视图标签扩展机制、SSMA 的空间-光谱联合掩码设计、特征级蒸馏对旧类表征漂移的直接约束，以及伪旧类特征生成中的全局尺度下界约束，均与本文具体代码实现保持一致，保证了方法描述与实验实现的严格对应。

上述方法框架为后续实验验证与性能分析奠定了完整的理论与算法基础，相关消融实验与对比结果将在第四章详细呈现。
